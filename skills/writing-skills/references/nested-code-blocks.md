# 多层代码块嵌套

写技能时经常要**示范一段 markdown**,而那段示例本身又含代码块 —— 这时内层的围栏会把外层
**提前闭合**,后面整篇的结构就乱了。

办法是**外层用更长的围栏**:外层四个反引号,内层三个。像这样:

````markdown
这个技能要求按下面那个模板写报告:

```bash
python scripts/x.py
```

模板结束。
````

要点:

- 外层围栏**必须比内层长**(四个反引号包住三个反引号),不然内层会先把外层关掉。
- 外层也要标语言 —— 上例标的是 `markdown`,因为示范的是一段 markdown。
- 检查器按 CommonMark 的规矩判闭合(闭合围栏**不短于**开启的那个),所以嵌套写法它认。
