---
name: writing-skills
description: 当用户想给这个 agent 增加一个新技能(或者修改已有技能),或者你想把一套固定的流程、领域知识沉淀下来复用的时候使用。它讲清了技能目录怎么写、description 该怎么措辞、markdown 格式有什么硬要求、什么样的内容才值得做成技能,以及交出去之前怎么自检。**只管「技能这个形式怎么写」** —— 写普通文档(README、报告、笔记)不必套技能的结构,那些直接写就行;技能正文里承载的领域知识也不由它负责。
---

# 编写技能

技能是**按需加载的说明书** —— 平时只占一行(名字 + 何时使用),需要时才把正文读进来。
所以它适合放"**只在特定场合才用得上的、成体系的**知识与流程",不适合放"任何时候都该遵守的纪律"
(那些属于系统提示词)。

## 目录结构

在项目根的 `skills/` 下建一个目录,目录名就是技能名:

```text
skills/<技能名>/
    SKILL.md          必需
    scripts/          可选:可执行脚本
    references/       可选:按需读进上下文的参考资料
    assets/           可选:产出里要用的文件(模板、图标)
```

`SKILL.md` 必须以一小段 frontmatter 开头,用 `---` 包起来:

```markdown
---
name: <技能名,和目录名一致>
description: <什么时候该用这个技能>
---

# 标题

正文……
```

## description 是关键,要写"什么时候用"

**清单里只显示 name 和 description** —— 模型就是靠这一行判断"现在该不该翻开它"。
所以要写**触发条件**,不是写"这个技能讲什么":

- ✅ 好:当用户要求审阅代码、或改完代码想检查有没有问题时使用。
- ❌ 差:关于代码审阅的说明。 —— 模型看完不知道什么时候该用

措辞上建议用第三人称陈述("……时使用"),而不是祈使句("请使用这个技能……")。

## 边界也要写进 description

模型**加载之前只看得到 description** —— 正文里的"目前只支持 .xlsx"那句,它翻开
之前根本看不到。边界不写在这里,它就会拿这个技能去处理根本不适用的输入,而且
**一路硬撑到出结果**,不会告诉你哪里没覆盖到。

判断标准:**只要有"某些输入处理不了",就写进 description**。

以"解析报表"这个技能为例,两版的差别:

- ❌ 差:当用户要求解析报表文件时使用,支持统计汇总与格式转换。
  —— 用户给个 `.xls`,它照样接下来,做到一半才发现解析不了
- ✅ 好:当用户要求解析报表文件时使用,支持统计汇总与格式转换。**目前只支持
  `.xlsx`**;`.xls` 和 CSV 请另想办法。

什么都能处理的技能(流程类、格式类)不用写这段。

**但只写"不适用于什么",别写"该找谁"。** 在 description 里指名道姓地写「见 xxx 技能」有两个
问题:对方一改名这句就**悄悄过期**(这几天改过两次技能名);而且它把清单撑长,读者要跳过一段
跟自己无关的话才到重点。**只写"不管什么"就够了** —— 模型知道这不是你管的事,自己会去翻别的
技能(清单里都列着)。

要指路就写在**正文**里 —— 翻开之后才需要它,不占清单的地方。

## markdown 格式有硬要求 —— 交出去之前先自查

技能是给人看的 markdown 文件。格式不规范的话,用户一打开就是满屏 lint 警告,很影响观感,
也显得不专业。

好消息:**写在工作区里的 markdown 会被自动检查** —— `write_file` / `append_file` /
`edit_lines` 写完 `.md` 会顺手 lint 一遍,有问题直接附在返回里。所以**看到那段提示就当场改掉**,
别拖到后面。要单独复查某个文件或整个目录,用 `check_markdown` 工具。

注意:技能文件(`skills/` 下)在**工作区之外**,agent 的工具够不到它 —— 那份由用户自己
(编辑器里就有 lint)把关。你只负责把内容写对、把上面的规范照做到。

检查覆盖 frontmatter 与 markdown 结构。最容易踩的几条(编号是 markdownlint 官方规则号,方便对照)。
**排最前面的两条是重灾区** —— 实测栽得最多的就是它俩:

- **MD032 列表上下都要空一行** —— 列表前面是段落文字时中间要空一行,列表后面同理。
  最常犯的写法就是写完"下面几点:"直接接列表。
- **MD036 别拿加粗当标题** —— `**第一步**` 整行加粗、自成一段就算违规。它是强调不是标题,
  要分小节就老实写 `### 第一步`。
- **MD022 标题上下都要空一行** —— `### 步骤` 下一行直接接内容就会报。
- **MD031 代码块上下都要空一行** —— 围栏前后都得空行。
- **MD040 代码块要标语言** —— ` ``` ` 后面写 `text` / `python` / `bash` / `markdown`
  都行,但**不能不写**。
- **MD041 / MD025** —— 正文第一个非空行要是一级标题;且全文**只能有一个** `#`。
- **MD012 / MD009 / MD047** —— 不要连续空行、行尾不要多余空格、文件结尾留且只留一个换行。
- **MD004 无序列表符号统一** —— 全文统一用 `-`,别混用 `*` 和 `+`。

## 多层代码块嵌套(容易忽略的坑)

写技能时经常要**示范一段 markdown**,而那段示例本身又含代码块 —— 这时内层的围栏会把外层
**提前闭合**,后面整篇的结构就乱了。

办法是**外层用更长的围栏**:外层四个反引号,内层三个。像这样:

````markdown
这个技能要求按下面这个模板写报告:

```bash
python /skills/<技能名>/scripts/x.py
```

模板结束。
````

要点:

- 外层围栏**必须比内层长**(四个反引号包住三个反引号),不然内层会先把外层关掉。
- 外层也要标语言 —— 上例标的是 `markdown`,因为示范的是一段 markdown。
- 检查器按 CommonMark 的规矩判闭合(闭合围栏不短于开启的),所以嵌套写法它认。

## 正文该写什么

写**具体怎么做**:步骤、判断依据、容易踩的坑、判断标准。可以引用工具名(比如
"先 `read_file` 看一眼")—— 技能和工具是配合关系,不是替代关系。

写的时候想清楚:模型**看到这篇正文之后**,行为会有什么不同?如果答案是"没什么不同",
那这篇就不值得存在。

## 脚本要用第三方包时

技能的脚本用了非标准库的包,正文里必须写清三件事:

1. **要装什么、怎么装** —— 包名和那行安装命令(`pip install --target /workspace/.pylibs <包>`,
   装一次持久保留)。
2. **装之前先问用户** —— 装包是**在动用户的环境**,不在"看代码"这个授权范围内。把「要装什么、
   装它做什么」讲清楚,由用户决定;不要自作主张装上。
3. **用户不同意时怎么办** —— 分两种,都要写明:是**整个做不了**(如 `repo-security-audit` 少了
   pyyaml 就一个候选都读不出来),还是**某一步降级**(少了 dulwich 只是 git 历史扫不了)。
   前者如实说"做不了",后者在报告的「未覆盖范围」里写明。

**最怕的是硬凑**:少了一个包就装作扫全了,用户拿到的是**看着像结论的猜测** —— 比明确说"这块没查"
糟糕得多。

现成的例子:`code-quality-check`(PHP 扫描器要 tree-sitter)、`repo-security-audit`(pyyaml / dulwich)。

## 附带资源

- `scripts/` —— **可以执行而不必读进上下文**。所以重的活(批量处理、格式转换)适合写成脚本,
  让模型只跑它、不读源码。
- `references/` —— 大段参考资料(规范、清单、示例集)。正文里点明"需要时去读哪个",
  而不是把内容整个抄进正文。
- `assets/` —— 产出里要被复制/引用的文件(模板、字体、图片)。

**这些文件是怎么被用到的**:技能目录在工作区**之外**,文件工具够不到它;但它被**只读**
挂载进容器,所以要用 `run_command` / `run_python` 走容器那条路:

```text
skills/<技能名>/scripts/foo.py   →   容器里:/skills/<技能名>/scripts/foo.py
```

正文里写"跑 `scripts/foo.py`"时,模型会自动换算成上面那个容器路径(`load_skill` 也会
把每个资源的实际路径列出来)。只读:能跑能读、改不了;VM 里看不到。

## 加完之后

技能是**自动发现**的:目录建好、`SKILL.md` 写好,下次启动就会出现在技能清单里,
不需要改任何代码或提示词。可以用 `load_skill("<技能名>")` 验证能不能读出来。

**交出去之前**跑一遍自检:

```bash
python3 /skills/writing-skills/scripts/check_skill.py           # 查全部技能
python3 /skills/writing-skills/scripts/check_skill.py <技能名>   # 只查一个
```

它查的都是**改名或挪文件之后才会暴露、而且不会报错**的那类问题:

- **交叉引用** —— `/skills/<名>/<路径>` 指向的技能和文件是否还在;正文里提到的 `某技能名`
  是否还有效(改名之后最容易留下的就是旧名字)
- **资源引用** —— 正文提到 `scripts/x.py`、`references/y.md`,那些文件是否真的存在
- **frontmatter** —— `name` 与目录名是否一致、`description` 写了没有
- **范围声明** —— description 里有没有交代"处理不了什么"(没写只是提醒,不算错)
- 正文行数是否过长、附带 Python 脚本有没有语法错误

**为什么非要机器查一遍**:技能**引用自己**是很常见的写法(正文里写
`/skills/<自己的名字>/scripts/x.py`)—— 改自己名字的时候,眼睛正盯着别处,这类必漏。
这个坑踩过两次,两次都是事后 grep 才发现的。

**动手写新技能之前**,先看一眼现有的引用关系,能少造一个重复的:

```bash
python3 /skills/writing-skills/scripts/check_skill.py --graph
```

它不给图、给**清单** —— 谁引用了谁(分「资源依赖」和「仅提及」两档:前者是真的复用对方的
文件、对方一改就坏;后者只是"细节见那边"的指路)、改某个技能要同步哪些地方,以及三类值得
看一眼的:互相引用(可能是在互相推诿,也可能是真配合,得人看)、被很多技能引用的枢纽
(改它影响面大)、与谁都不往来的孤岛。
